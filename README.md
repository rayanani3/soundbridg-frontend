# SoundBridg Frontend

Vite + React + TailwindCSS frontend for soundbridg.com

## Setup

```bash
npm install
npm run dev
```

## Deploy (Netlify)

Build command: `npm run build`
Publish directory: `dist`

## Environment

No env vars needed — backend URL is hardcoded to the Render deployment.
